type User {
    _id: String!
    type: String!
    name: String
    # email: String
    # mobile_number: Int
    uniqueId: String
    tokens: [String]
    customer: Customer
}

type Query {
    user(name: String): User
}