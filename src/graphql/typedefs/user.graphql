type User {
    _id: String!
    name: String
    # email: String
    # mobile_number: Int
    uniqueId: String
    tokens: [String]
    is_luggage_stored: Boolean
    bookings: [Booking]
}

type Query {
    user(name: String): User
}