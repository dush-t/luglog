type User {
    name: String
    # email: String
    # mobile_number: Int
    uniqueId: String
    tokens: [String]
    is_luggage_stored: Boolean
    bookings: [Booking]
}

type Query {
    user(mobile_number: String): User
}